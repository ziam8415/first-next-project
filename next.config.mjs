/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ["i.ibb.co.com", "images.unsplash.com"],
  },
};

export default nextConfig;
